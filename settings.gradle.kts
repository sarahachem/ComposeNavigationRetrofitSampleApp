dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        maven(url = uri("https://www.jitpack.io"))
    }
}
rootProject.name = "Dog Breeds App"
include(":app")
